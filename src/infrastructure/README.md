# `src/infrastructure/`

Accès **externe** et persistance : le domain ne connaît pas React Native ni les SDK.

Implémentations concrètes (AsyncStorage, Supabase) + repositories qui exposent des interfaces simples aux features.

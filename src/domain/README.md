# `src/domain/`

Logique métier **pure** : types, règles, calculs, fonctions sans effet de bord.

**Interdit** : React, composants, AsyncStorage, Supabase, navigation.

**Exemple** : `planning/generateDailyPlan.ts` — pas dans un écran.

Les sous-dossiers regroupent le domaine par concept métier.

# `shared/components/`

Composants UI **génériques** réutilisables par toutes les features.

Pas de dépendance vers `features/` ni `domain/`.

Contient aussi les composants template Expo (à remplacer progressivement par le design system GoalFlow).

# `shared/theme/`

**Design system** : couleurs, typographie, espacements, tokens dark/light.

Palette GoalFlow dans `colors.ts` (fond chaud `#FFFBF5`, accent corail `#F97316`), `spacing.ts`, `typography.ts`.

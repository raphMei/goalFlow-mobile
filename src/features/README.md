# `src/features/`

**Écrans** et composants liés à une fonctionnalité produit.

Chaque sous-dossier = une feature (today, goals, …) avec ses screens, composants locaux et hooks spécifiques.

Les routes dans `src/app/` importent depuis ici. Pas de logique métier complexe : déléguer à `domain/`.

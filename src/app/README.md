# `src/app/`

Navigation **Expo Router** : routes, layouts, redirections.

**Contient** : `_layout.tsx`, groupes de routes `(tabs)/`, modales.

**Ne contient pas** : logique métier, appels API, règles de planification.

Les écrans métier vivent dans `src/features/` et sont importés ici.
